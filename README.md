# Digit-identifier

A Matlab code that trains a neural network using MNIST digit data to identify handwritten digits from images. The neural network is built using a gradient descent method, the number of nodes can be set (default number of layers 3).
